import React from "react";

const AdminSidebar = ({ styles }) => {
  return (
    <aside
      className={`${styles} w-[200px] p-4 bg-gray-400 h-full relative transition-all duration-200 ease-linear`}
    >
      hello class
    </aside>
  );
};

export default AdminSidebar;
